<!-- SiteMaintenanceView.xaml -->
<Window x:Class="KitchenEquipmentManagement.Frontend.WPF.Views.SiteMaintenanceView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KitchenEquipmentManagement.Frontend.WPF.Views"
        mc:Ignorable="d"
        Title="Site Maintenance" Height="300" Width="400">
    <Grid>
        <DataGrid ItemsSource="{Binding Sites}" SelectedItem="{Binding SelectedSite}" AutoGenerateColumns="False" Margin="10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding SiteId}" IsReadOnly="True" />
                <DataGridTextColumn Header="Name" Binding="{Binding Description}" />
                <DataGridTextColumn Header="Active" Binding="{Binding Active}" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="10">
            <Button Content="Add" Command="{Binding AddCommand}" Margin="5"/>
            <Button Content="Edit" Command="{Binding EditCommand}" Margin="5"/>
            <Button Content="Delete" Command="{Binding DeleteCommand}" Margin="5"/>
            <Button Content="Edit Site Equipment" Command="{Binding EditSiteEquipmentCommand}" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>
