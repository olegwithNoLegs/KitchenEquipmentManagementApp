<Window x:Class="KitchenEquipmentManagement.Frontend.WPF.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KitchenEquipmentManagement.Frontend.WPF.Converters"
        xmlns:helpers="clr-namespace:KitchenEquipmentManagement.Frontend.WPF.Helpers"
        mc:Ignorable="d"
        Title="Login" Height="300" Width="400" WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <local:StringNullOrEmptyToVisibilityConverter x:Key="StringNullOrEmptyToVisibilityConverter"/>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Username Field -->
        <Grid Margin="0 0 0 10">
            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" 
                     Padding="5"
                     VerticalContentAlignment="Center"/>
            <TextBlock Text="Username"
                       Foreground="Gray"
                       IsHitTestVisible="False"
                       Margin="10,0,0,0"
                       VerticalAlignment="Center"
                       Visibility="{Binding Username, Converter={StaticResource StringNullOrEmptyToVisibilityConverter}}"/>
        </Grid>

        <!-- Password Field -->
        <Grid Grid.Row="1" Margin="0 0 0 10">
            <PasswordBox
                helpers:PasswordBoxHelper.BindPassword="True"
                helpers:PasswordBoxHelper.BoundPassword="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                Padding="5"
                VerticalContentAlignment="Center"/>
            <TextBlock Text="Password"
                       Foreground="Gray"
                       IsHitTestVisible="False"
                       Margin="10,0,0,0"
                       VerticalAlignment="Center"
                       Visibility="{Binding Password, Converter={StaticResource StringNullOrEmptyToVisibilityConverter}}"/>
        </Grid>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Login" Margin="0,10,10,0" Command="{Binding LoginCommand}" Width="100"/>
            <Button Content="Signup" Margin="0,10,0,0" Command="{Binding SignupCommand}" Width="100"/>
        </StackPanel>
    </Grid>
</Window>
