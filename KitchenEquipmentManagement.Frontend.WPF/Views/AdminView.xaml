<!-- AdminView.xaml -->
<Window x:Class="KitchenEquipmentManagement.Frontend.WPF.Views.AdminView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KitchenEquipmentManagement.Frontend.WPF.Views"
        mc:Ignorable="d"
        Title="Admin" Height="300" Width="400">
    <StackPanel Margin="20">
        <Button Content="Users" Command="{Binding NavigateUsersCommand}" IsEnabled="{Binding IsSuperAdmin}" Margin="0 0 0 10"/>
        <Button Content="Sites" Command="{Binding NavigateSitesCommand}" Margin="0 0 0 10"/>
        <Button Content="Equipments" Command="{Binding NavigateEquipmentsCommand}" Margin="0 0 0 10"/>
        <Button Content="Logout" Command="{Binding LogoutCommand}" />
    </StackPanel>
</Window>
