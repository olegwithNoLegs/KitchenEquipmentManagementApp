<Window x:Class="KitchenEquipmentManagement.Frontend.WPF.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="300" Width="400">
    <Grid Margin="20">
        <StackPanel>
            <TextBox PlaceholderText="Username" Text="{Binding Username}" Margin="0,10"/>
            <PasswordBox x:Name="PasswordBox" Margin="0,10" PasswordChanged="PasswordBox_PasswordChanged"/>
            <Button Content="Login" Command="{Binding LoginCommand}" Margin="0,20"/>
            <Button Content="Sign Up" Command="{Binding SignUpCommand}" />
        </StackPanel>
    </Grid>
</Window>
